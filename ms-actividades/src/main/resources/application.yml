server:
  port: 8083

spring:
  application:
    name: ms-actividades
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:actividades.db
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
;